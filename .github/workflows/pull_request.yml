name: CI for Pull Requests

on:
  - pull_request

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1

      - name: Build the Docker image
        run: docker build src/docker --tag my-image-name:$(date +%s)
